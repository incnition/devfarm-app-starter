module.exports.mutations = {
  Mutation: {
    createUserWithEmail: async (_, { email, password, passwordConfirmation, admin, firstName, lastName }) => {
      // TODO: create a user
    },
    updateUser: async (_, { email, password, passwordConfirmation, admin, firstName, lastName }) => {
      // TODO: update a user
    }
  }
}
